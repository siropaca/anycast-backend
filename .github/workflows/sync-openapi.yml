name: Sync OpenAPI to Frontend

on:
  push:
    branches:
      - main
    paths:
      - "swagger/openapi.json"
  workflow_dispatch:

jobs:
  sync:
    name: Sync openapi.json to frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout backend
        uses: actions/checkout@v4

      - name: Checkout frontend
        uses: actions/checkout@v4
        with:
          repository: siropaca/anycast-forntend
          token: ${{ secrets.FRONTEND_REPO_TOKEN }}
          path: frontend

      - name: Copy openapi.json
        run: cp swagger/openapi.json frontend/openapi.json

      - name: Check for changes
        id: check
        working-directory: frontend
        run: |
          git add openapi.json
          if git diff --cached --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.check.outputs.changed == 'true'
        id: create-pr
        working-directory: frontend
        env:
          GH_TOKEN: ${{ secrets.FRONTEND_REPO_TOKEN }}
        run: |
          BRANCH_NAME="chore/sync-openapi-$(date +%Y%m%d%H%M%S)"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git checkout -b "$BRANCH_NAME"
          git add openapi.json
          git commit -m "chore: sync openapi.json from backend"
          git push origin "$BRANCH_NAME"

          PR_URL=$(gh pr create \
            --title "chore: sync openapi.json from backend" \
            --body "Backend の openapi.json を同期します。" \
            --base main \
            --head "$BRANCH_NAME")

          echo "pr_url=$PR_URL" >> $GITHUB_OUTPUT

      - name: Merge Pull Request
        if: steps.check.outputs.changed == 'true'
        working-directory: frontend
        env:
          GH_TOKEN: ${{ secrets.FRONTEND_REPO_TOKEN }}
        run: |
          gh pr merge --squash --delete-branch "${{ steps.create-pr.outputs.pr_url }}"
