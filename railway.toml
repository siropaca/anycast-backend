[build]
builder = "nixpacks"

[deploy]
startCommand = "migrate -path migrations -database \"$DATABASE_URL\" up && ./bin/server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
