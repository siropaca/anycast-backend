# ===================
# App
# ===================
APP_ENV=development
PORT=8081

# ===================
# Database
# ===================
DATABASE_URL=postgres://postgres:postgres@localhost:5433/anycast?sslmode=disable
# DB クエリのログレベル（silent, error, warn, info）デフォルト: silent
DB_LOG_LEVEL=silent

# ===================
# CORS
# ===================
# カンマ区切りで複数指定可能
CORS_ALLOWED_ORIGINS=http://localhost:3210

# ===================
# Auth
# ===================
# フロントエンドの AUTH_SECRET と同じ値を設定
AUTH_SECRET=

# ===================
# LLM
# ===================
# OpenAI API キー
OPENAI_API_KEY=
# Claude API キー
CLAUDE_API_KEY=
# Gemini LLM のロケーション（デフォルト: asia-northeast1）
GEMINI_LLM_LOCATION=

# ===================
# Image Generation
# ===================
# 画像生成プロバイダ（gemini / openai）デフォルト: gemini
IMAGE_GEN_PROVIDER=
# OpenAI 画像生成モデル（デフォルト: gpt-image-1）
OPENAI_IMAGE_GEN_MODEL=

# ===================
# TTS
# ===================
# TTS プロバイダ（gemini / elevenlabs）デフォルト: gemini
TTS_PROVIDER=
# ElevenLabs API キー（TTS_PROVIDER=elevenlabs の場合に必要）
ELEVENLABS_API_KEY=

# ===================
# Google Cloud
# ===================
# GCP プロジェクト ID
GOOGLE_CLOUD_PROJECT_ID=
# サービスアカウントの JSON キー（Railway などで使用）
# ローカル開発では空欄のままで gcloud auth application-default login を使用
GOOGLE_CLOUD_CREDENTIALS_JSON=
# GCS バケット名
GOOGLE_CLOUD_STORAGE_BUCKET_NAME=
# Gemini TTS のロケーション（デフォルト: global）
GOOGLE_CLOUD_TTS_LOCATION=
# ローカル開発時は以下を空欄にすると、Cloud Tasks を使わずに goroutine で直接実行されます
# Cloud Tasks のロケーション
GOOGLE_CLOUD_TASKS_LOCATION=
# Cloud Tasks のキュー名
GOOGLE_CLOUD_TASKS_QUEUE_NAME=
# Cloud Tasks が使用するサービスアカウントのメールアドレス（OIDC 認証用）
GOOGLE_CLOUD_TASKS_SERVICE_ACCOUNT_EMAIL=
# Cloud Tasks から呼び出されるワーカーエンドポイントの URL
GOOGLE_CLOUD_TASKS_WORKER_URL=

# ===================
# Trace
# ===================
# トレースモード（none, log, file）デフォルト: none
TRACE_MODE=none

# ===================
# Slack
# ===================
# Slack Webhook URL（フィードバック通知用、空の場合は通知無効）
SLACK_FEEDBACK_WEBHOOK_URL=
# Slack Webhook URL（お問い合わせ通知用、空の場合は通知無効）
SLACK_CONTACT_WEBHOOK_URL=
# Slack Webhook URL（ジョブ失敗アラート通知用、空の場合はアラート無効）
SLACK_ALERT_WEBHOOK_URL=
