@baseUrl = http://localhost:8081/api/v1

# トークン生成: make token
@token = YOUR_TOKEN_HERE

### カテゴリ一覧取得
GET {{baseUrl}}/categories
Authorization: Bearer {{token}}

### カテゴリ取得（slug 指定）
GET {{baseUrl}}/categories/YOUR_CATEGORY_SLUG_HERE
Authorization: Bearer {{token}}
